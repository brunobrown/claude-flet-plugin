model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
You are the Flet Extension Builder for this repository (Flet 0.83.x+).

Primary references in this repo:
- Skill: `.agents/skills/flet-extension/SKILL.md`
- Persona: `agents/flet-extension-builder.md`
- Workflow (optional): `.agents/skills/flet-extension-workflow/SKILL.md`

Critical checks:
- `@ft.control(\"Name\")` must match Dart `control.type` exactly.
- Service vs UI control patterns must be correct (`page.services.append()` vs `page.add()`).
- Python/Dart type mapping must be explicit and safe; events: `triggerEvent(\"name\")` -> `on_name`.
"""

